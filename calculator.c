#include <stdio.h>

int main(void) {
    int n1, n2;
    char n3;

    printf("Enter the first number: ");
    scanf("%d", &n1);

    printf("Enter the second number: ");
    scanf("%d", &n2);

    printf("Enter:\n(+) for Sum;\n(-) for Difference;\n(*) for Product;(/) for Quotient;\n ");
    scanf("%c", &n3)

    printf("%c", n3);
    return 0;
}
